# 보안과 운영 한계

## GitHub Pages

GitHub Pages는 정적 공개 사이트입니다. 저장소가 공개이면 HTML과 `collector-config.js`도 누구나 볼 수 있습니다. 수집된 JSON은 GitHub 저장소에 저장되지 않으며, 설정한 외부 수집기로 전송됩니다.

## study token

`collector-config.js`의 study token은 브라우저에서 읽을 수 있으므로 비밀 인증정보가 아닙니다. 다음 용도로만 사용합니다.

- 다른 프로젝트의 우발적 제출 차단
- 잘못된 endpoint 연결 감지
- 간단한 intake namespace 구분

공격적 스팸이나 악의적 제출을 막는 보안 수단으로 보아서는 안 됩니다.

## 권장 보호 조치

- 참가자 코드에 실명·이메일을 넣지 않음
- 파일럿 URL을 필요한 사람에게만 공유
- Drive 폴더를 비공개로 유지
- 제출 Sheet와 Drive 접근 권한을 최소화
- 이상 제출이 늘어나면 token을 교체하고 웹 앱을 재배포
- Apps Script 실행 로그와 할당량을 주기적으로 확인
- 본 연구 전 시험 세션을 삭제하거나 별도 폴더로 이동

## 더 큰 연구

대규모 공개 모집, 민감정보 수집, 장기 서비스에는 다음이 필요할 수 있습니다.

- 인증된 API
- rate limiting
- 서버 측 비밀키
- 데이터베이스 접근제어
- 암호화와 감사 로그
- 기관이 승인한 저장소

이 경우 GitHub Pages는 프런트엔드로 유지할 수 있지만, Apps Script 대신 별도 백엔드를 사용하는 편이 적절합니다.
