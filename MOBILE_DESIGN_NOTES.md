# 반응형 화면 설계 메모

CUBE-REV는 같은 실험 로직을 유지하면서 화면 크기와 입력 장치에 따라 배치만 바꿉니다.

## 화면 구분

```text
휴대폰: 720 CSS px 이하
태블릿: 721–1100 CSS px
데스크탑: 1101 CSS px 이상
```

분류에는 확대·축소에 따라 달라지는 visual viewport 너비가 아니라 layout viewport 너비를 사용합니다. 높이는 모바일 주소창과 하단 브라우저 막대를 반영하기 위해 visual viewport를 사용합니다.

## 휴대폰

- 큐브가 남은 화면 높이를 최대한 사용합니다.
- 시행 정보는 위쪽 2×2 카드로 정리됩니다.
- 되돌리기·초기화·조작·세션 종료·시행 포기는 화면 아래에 고정됩니다.
- 직접 회전 버튼과 JSON 저장은 `조작` 버튼을 눌러 여는 하단 시트에 들어갑니다.
- 노치와 홈 인디케이터를 피하도록 safe-area inset을 반영합니다.
- 주요 터치 버튼은 최소 48 CSS px 높이를 확보합니다.

## 태블릿

- 큐브를 위에 두고 조작 패널을 아래 두 열로 배치합니다.
- 세로 공간이 모자라면 실험 본문만 스크롤됩니다.
- 가로 스크롤은 생기지 않습니다.

## 데스크탑

- 기존의 큐브 왼쪽·조작 패널 오른쪽 배치를 유지합니다.
- 실험 화면은 브라우저 높이 안에 고정됩니다.
- 조작 패널이 길어지면 오른쪽 패널만 독립적으로 스크롤됩니다.

## 입력 감도

```text
마우스 드래그 최소 거리: 42 px
펜 드래그 최소 거리: 36 px
손가락 드래그 최소 거리: 34 px
```

손가락은 마우스보다 시작점이 흔들리기 쉬우므로 거리 기준만 조금 낮췄습니다. 방향 일치도·후보 간 점수 차이·경로 직진성 기준은 유지합니다. 한 번의 드래그는 여전히 한 번의 90° 회전만 만들며 `R2` 같은 2회전을 자동 생성하지 않습니다.

## 코드 역할

- `cube-drag-controller.js`: 마우스·펜·터치 제스처와 면 회전 판정
- `collector-client.js`: 수집기 상태 확인, 제출, 저장 영수증 조회
- `responsive-layout-controller.js`: 화면 분류, 모바일 조작 시트, safe viewport 관리
- `index.html`: 실험 상태와 세 구성요소 연결

각 모듈이 다른 모듈의 내부 상태를 직접 수정하지 않고, 전달받은 함수만 호출하도록 나눴습니다.
