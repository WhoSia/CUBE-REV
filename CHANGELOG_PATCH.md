# CUBE-REV 0.6.11 마무리 패치

## 수집 확인

- 단순 `no-cors` 전송 완료 표시 제거
- 제출 전에 Apps Script 버전·study token·receipt 기능 확인
- `POST → Drive 저장 → receipt 발급 → JSONP receipt 조회` 절차 추가
- Drive 저장이 확인된 뒤에만 완료 화면에 `수신 완료` 표시
- 저장 파일명과 수신 확인 코드 표시
- receipt가 확인되지 않으면 새 세션·창 닫기 경고 유지
- 자동 제출 중에는 불필요한 수동 제출 버튼을 숨기고, 실패했을 때만 다시 표시

## 모바일·태블릿

- 휴대폰용 전체 높이 실험 화면
- 안전 영역과 모바일 브라우저 동적 높이 반영
- 휴대폰 하단 고정 작업 막대
- 조작 버튼용 하단 시트
- 태블릿 2열 조작 패널
- 데스크탑 조작 패널 독립 스크롤
- 터치·펜·마우스별 드래그 거리 기준 분리

## 유지된 항목

- 공개 버전 `0.6.11`
- 면 드래그는 한 번에 90° 한 번만 수행
- 시행 시간 상한 이후 자동 이동 없음
- 상대 시간만 `max` 처리
- 기존 Google Drive 폴더와 제출 색인 Sheets 재사용
