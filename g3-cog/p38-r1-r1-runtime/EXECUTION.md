# G3-P38-R1-R1 terminal execution

Canonical Notion authority: `3e5ef561-cf92-8168-94aa-e66e68c87389` (the later-edited of two byte-equivalent duplicate packet pages).

Verdict: `P38_R1_R1_PASS / CANONICAL_OBSERVATION_AUTHORITY_RECONSTITUTED / YPLUS_ORBIT_CLOSED_DOMAIN_CERTIFIED / RULE_SELECTED_WITNESS_CERTIFIED / FULL_DOMAIN_SEARCH_REUSE_CERTIFIED / TRANSPORT_REVIEW_REAUTHORIZED / WORLD_CONTACT_HOLD / NO_SUCCESSOR_OPENED`.

Commands:

```text
node --max-old-space-size=6144 g3-cog/p38-r1-r1-runtime/run.mjs
node g3-cog/p38-r1-r1-runtime/verify.mjs
```

The production run was repeated after correcting a terminal-receipt bookkeeping field; both runs independently selected the same color support and witness and returned zero raw-control failures. The final artifacts are from the corrected full replay.

## Canonical observation

The scientific observation is the canonical adapter's 54-position color serialization. Across 155,040 states, 16 targets and 2,480,624 target-rival comparisons, the full compressed frontier contains 2,318,194 distinct color supports. The semantic-coarsening inclusion had zero exhaustive violations. Minimum cardinality is 6, with two candidates. Both have one affected target (13,648 contexts), one merged pair and maximum fiber 2. Frozen lexicographic tie-break selects `D_color = [4,8,9,11,31,45]`. Removing any selected coordinate separates the selected pair and returns zero coverage. Its verified 24-rotation orbit has size 24 and stabilizer 1; only the selected member occurs on this finite frontier.

The historical ID minimum-10 frontier and `[3,4,21,22,24,25,28,29,41,42]` remain preserved as latent engineering/history results. They are not re-labelled as scientific color observations.

## Orbit-closed domain and witness

The 218,368 original row-history contexts expand without deduplication to 873,472 exact `g^0..g^3` contexts. The domain is closed and `g` is a bijection. It contains 4,096 directional boundaries, split 2,048/2,048, with every record paired and residue 0. The old 16-origin result—0 matched and 1,024 unmatched—remains preserved as the non-closed restriction.

The executable frozen comparator selects `P34-001644/A/r2`, budget 6, history length 6, selector symmetric difference 4, first divergent prefix 1, origin hash `1818f50508e8a4fdaa8c2d52e25173a4b17e26af4d54d0666689e08e6ab2750f`. Historical `P34-001164/A/r2` remains a valid route but not the rule-selected first witness.

## Solved-origin reuse

The actual normalization is the value relabeling `N_q(s)[i]=q^{-1}(s[i])`. Since repository moves permute positions only, `N_q(T(s,a))=T(N_q(s),a)` for the literal move. The map is invertible and preserves cost, budget, same-face grammar, used-face masks, literal selector membership and route-mask antichains.

Exhaustive checks cover 218,368 contexts, all 6,724 reuse keys, 2,620,416 move-transition identities, 218,368 inversions, 1,311,232 grammar adjacencies and 218,368 mask identities, with zero failure. Independent unnormalized controls include the first available row for all 16 targets at each present budget 6/7/8 plus historical/canonical edge witnesses, both representations and selector masks 1,3,7,14,15,31,62: 100 raw/normalized antichain comparisons, zero failure.

## Boundary

All four R1-R1 gates pass, so P38 transport review is reauthorized. This is a finite-domain formal result only: it does not validate an external domain, a cognition claim, or world contact. No successor was opened. P38-R1 HOLD remains historical audit evidence rather than being overwritten.
